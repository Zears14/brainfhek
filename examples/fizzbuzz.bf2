#include "stdlib"

// Print 1..15 with newlines (FizzBuzz-style driver; full divisibility left as exercise)
fn main() -> i32 {
    seg i { i32[1] }
    i[0] = 1
    {15} {
        load i[0]
        .i
        = 10
        .
        i[0] +1
    }
    ret 0
}
